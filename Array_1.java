import util.java.Array_1

class Array_1{

public int findpeakelement(int arr[], arrlength){
	if (arrlength==1);
	return arr[0];
	if (arr[0]>=arr[1]);
	return arr[0;
	if (arr[arrlength-1]>=arr[arrlength-2]);
	return arr[arrlength-1];
	
	for(int i=1; i=arrlength-1;i++){
		if (arr[i]>=arr[i-1] && arr[i]>=arr[i-1]);
		return arr[i];
		
	}
	return arr[0];
}
}
	